<div class="nav">
    <ul>
    	<li>
        	<a href="http://localhost:3000/">Pawsome</a>
      	</li>

      	<li>
        	<a href="http://localhost:3000/survey">Survey</a>
      	</li>
      	<li>
        	<a href="http://localhost:3000/shelters">Shelters</a>
      	</li>
      	<li>
        	<a href="http://localhost:3000/quotes">Express Yourself</a>
      	</li>
     </ul>
</div>


<div class="container-fluid col-xs-10 col-xs-offset-1 text-center">
{{#each pets}}
<div class="pet col-xs-11">
<img src="{{media.photos.[1].pn}}">

	<h3 class="info">Name: {{name}}</h3>
	<p class="info">Sex: {{sex}}</p>
	<p class="info">Age: {{age}}</p>
	<p class="info">Size: {{size}}</p>
	<p class="info">Breed: {{breeds}}</p>
	<p class="info">{{description}}</p>
	<p class="info">City: {{contact.city}}</p>
	<p class="info">Phone: {{contact.phone}}</p>
	<p class="info">Email: {{contact.email}}</p>
	<form method="post" action="/list">
	<button type="button" class=" btn btn-success col-xs-4 col-xs-offset-4 add">Add</button>
	</form>
</div>
{{/each}}
</div>
